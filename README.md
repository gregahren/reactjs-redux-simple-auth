# ReactJS Redux client authentication boileplate

For backend this proj uses https://github.com/gregahren/mongodb-nodejs-auth-starter

You can start app by entering in terminal (in project root directory):
```
> npm install
> npm start
```